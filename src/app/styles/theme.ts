import { colors } from "@app/styles/colors.ts";
import { fonts } from "@app/styles/font.ts";
import { breakpoints } from "@app/styles/break-points.ts";

const theme = {
  colors,
  fonts,
  breakpoints,
};

export default theme
